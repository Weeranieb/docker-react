language: generic
sudo: required
services: 
  - docker

before_install: 
  - docker build -t Weeranieb/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true Weeranieb/docker-react npm run test
