language: generic
sudo: required
services: 
  - docker

before_install: 
  - docker build -t weeranieb/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true weeranieb/docker-react npm run test -- --coverage
